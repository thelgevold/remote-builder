version: "3.5"

networks:
  remote-build_remote-server:
    external: true

services:
  frontend:
    build: .
    ports:
      - "5432:5432"
      - "35729:35729"
    container_name: frontend
    volumes:
      - ../angular-demo:/usr/angular-bazel-example
    networks:
       remote-build_remote-server: